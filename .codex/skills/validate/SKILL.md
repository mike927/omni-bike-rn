---
name: validate
description: Run the project's full validation suite (lint, typecheck, tests, format). Use when the user asks to validate, run checks, verify a change before commit/PR, or any phrasing like "run the validations", "check the branch", "is this ready to commit". Also use when the feature workflow reaches Step 7 (Validation Complete).
---

Read and execute the procedure in `ai/commands/validate/COMMAND.md`.
