---
name: start-feature
description: Set up the workspace for a new feature — confirm branch name, create an in-place branch (default) or worktree (on request), and print the branch slug. Use when starting new work, or any phrasing like "start a new feature", "create a branch for X", "let's begin X", "kick off this task". Also use when the feature workflow reaches Step 2 (Workspace Preparing).
---

Read and execute the procedure in `ai/commands/start-feature/COMMAND.md`.
