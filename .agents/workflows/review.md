---
description: Run an internal code review against the current branch diff before opening a PR.
---
Read and execute the procedure in `ai/commands/review/COMMAND.md`. Note that the default scope is `branch` unless otherwise specified by the user.
