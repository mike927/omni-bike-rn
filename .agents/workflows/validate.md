---
description: Run the full validation suite and report pass/fail per command.
---
Read and execute the procedure in `ai/commands/validate/COMMAND.md`. Note that the default scope is `full` unless otherwise specified by the user.
