---
description: Mark plan.md complete, merge the PR via GitHub CLI (merge commit), verify the local workspace is clean, remove the branch or worktree, and return to main.
---
Read and execute the procedure in `ai/commands/finish-feature/COMMAND.md`.
