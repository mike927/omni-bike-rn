---
description: Set up the workspace for a new feature — confirm branch name, ask workspace strategy interactively, create the branch or worktree, and print the branch slug for downstream artifacts.
---
Read and execute the procedure in `ai/commands/start-feature/COMMAND.md`.
