# Strava API credentials
# Register your application at https://www.strava.com/settings/api to obtain these values.
# For local development, copy this file to .env and fill in the values.
# For CI/EAS builds, set these as EAS secrets instead.
STRAVA_CLIENT_ID=
STRAVA_CLIENT_SECRET=
