description = "Work through code review findings (local review file or GitHub PR threads), fix each actionable item using the Fix Loop Decision Rules, and prepare reply text per thread."
prompt = """
Execute the procedure defined in @{ai/commands/address-code-review/COMMAND.md}.

{{args}}
"""
