# Gemini Entry Point

@AGENTS.md

## Gemini-Specific Notes

- When asked to run a command, match user intent against the `triggers` field in `ai/commands/*/COMMAND.md` frontmatter — all logic lives in the canonical command files
- Use `GEMINI.local.md` for personal preferences not shared with the team (git-ignored)
- Start complex tasks with a plan before writing code
